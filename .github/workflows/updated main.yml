name: Deploy to S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{AKIAWKON7BF5RMO3E46O}}
          aws-secret-access-key: ${{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}}
          aws-region: us-east-1

      - name: Deploy to S3
        run: |
          aws s3 sync . s3://staticwebsitetarcin --delete --acl public-read
